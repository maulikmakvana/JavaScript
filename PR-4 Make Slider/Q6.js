// 6 : Create an array of product objects with name, price, quantity. Find total value of stock (price * quantity for all products).
console.log("6 : Create an array of product objects with name, price, quantity. Find total value of stock (price * quantity for all products).") ;


let markss = {
  math: 80,
  science: 90,
  english: 85
};

let totall = marks.math + marks.science + marks.english;

console.log("Total Marks:", total);

